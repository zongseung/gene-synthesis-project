# SDD ledger — plan: docs/superpowers/plans/2026-09-15-hipodit-ld.md

Spec: none reachable (the plan is a self-contained 기획서; rulings are provisional against plan §1–§8).
Branch: hipodit-ld (from dd670c0). Foundation commit c6b5f02 = uncommitted root-project HiPoDiT rebuild files (src/, tests/, scripts/hipodit_*.py, configs/default.yaml, README.md, uv.lock, OC-FILM deletions). korean-medicine-llm/ and misc untracked files left untouched. NOTE: `docs/` is gitignored — the plan file and docs/reports are not tracked.
Workspace: .superpowers/sdd/2026-09-15-hipodit-ld/
Envs: tests/CPU = `.venv/bin/python` (3.13, torch 2.13 CPU-only, sklearn 1.9); GPU train = `/home/user/Envs/csdi/bin/python` (3.10, torch 2.4.1+cu121, numpy 2.1.1, sklearn 1.5.2, no pytest). New code must run in both. GPUs: 2× RTX A6000 idle.
Frozen prepared data: outputs/diagnostics/hipodit_fisher_20260915_unique — 8 chr17 genes / 361 SNPs / 2,504 people, split 2,002/251/251 (train/dev=val/test), 26 cohorts (train 49–91 each, dev 6–11 each), NO missing calls, positions strictly ascending within each gene, offsets [0,23,87,104,157,197,261,315,361], train adjacent-distance quartile edges [35,104,210] → 4 bins with 86/89/89/89 pairs; train MAF bins [.01,.05)/[.05,.2)/[.2,.5]: 140/122/99 SNPs. Files: dataset.npz (x normalized (2504,8,4), y, split indices), genotypes.npz (calls float32 NaN=missing, offsets), gene_variant_map.json (positions), glm_pca_parameters.pkl (per-gene loadings (J_g,4)/intercept), normalization_stats.pkl, label_hierarchy.pkl, split_manifest.json, prepare_report.json.
Pilot: outputs/diagnostics/hipodit_multiseed_20260915 (5 seeds × standard/fisher, 1000 steps, 251 val samples, 100 DDIM; numbers match plan §1). B0 decoder = scripts/hipodit_genotype_check.py::evaluate_genotypes (independent Binomial draw, seed+1). Train child = `scripts/hipodit_rebuild_check.py train` run under csdi python; ~22 s per run.
Baseline tests: `.venv/bin/python -m pytest -q -p no:cacheprovider tests/` → 104 passed, 1 pre-existing CUDA warning.

## Task map (plan has Phase headings, not "Task N" → briefs hand-written; R2)
- T1 core module: src/models/genotype_decoder.py + tests/test_genotype_decoder.py (Phase 0 leakage assertions + Phase 1 likelihood/sampling)
- T2 Phase 0+1: scripts/hipodit_genotype_check.py — metric functions + `oracle` CLI (study_manifest.json, B0–B3 fit on train, dev metrics, λ grid on dev, negative controls, Gate 1 verdict) + actually run it
- T3 Phase 2: hipodit_rebuild_train.py / evaluate_genotypes decoder hookup (--decoder-dir, --eval-split), one GPU seed, Gate 2 verdict
- T4 Phase 3: hipodit_multiseed(+summary) B0/B3 paired arms on standard schedule, ≥10 seeds, bootstrap CI, test split opened once; B3-F once if gates pass
- T5 Phase 4: privacy (NN distance, MIA AUC, exact/near duplicate) + strata table (cohort n / MAF bin / `not estimable`)
- T6 Phase 5: docs/reports/hipodit_ld_20260915.md results report (R5: not a manuscript)
Gate rule: a failed gate stops downstream phases (plan header); T6 still reports the negative result.

## Pre-flight scan
| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1↔T2 | decoder API (distance_bins, fit_decoder, call_logits, nll_calls, sample_calls, save/load) | fixed in T1 brief; T2 brief carries identical signatures — OK |
| T2↔T3 | decoder_B{0,1,2,3}.npz in oracle out dir; evaluate_genotypes gains decoder_dir + split | T1 save/load fixes format; T3 consumes — OK |
| T3↔T4 | report keys synthetic (B0, unchanged) / synthetic_B3 / real_latent_decoder_reference(_B3); CLI --decoder-dir/--eval-split; synthetic_genotypes(_B3).npy | existing summary's same-seed reference check remains valid because the B0 draw seed is unchanged — OK |
| T4↔tests/test_hipodit_multiseed.py | SCHEDULES default (standard, fisher) | keep default; decoder arms optional — OK |
| T5↔T3/T4 | per-seed synthetic_genotypes_B3.npy needed | T3 must save it — carried into T3 brief |
| T1 self | plan §3.3 formula vs tests | log Binom(g;2,σ(η)) = logC(2,g)+gη−2softplus(η); softplus cancels in softmax → logits gη+logC+A; NLL convex in (U,A) — consistent |
| T2 self | Gate 1 on dev only; test untouched until T4 | consistent |
| T4 self | "test NLL paired difference 95% CI" across seeds but NLL is seed-independent | CONFLICT → R3 |
| T4 self | "real-vs-synthetic population-classifier AUC/accuracy" ambiguous | → R4 |
| T6 self | Phase 5 = manuscript sections | → R5 |
| plan §6 | no file owns study_manifest | → R6 |
| plan §3.3/§5 | λ_u, λ_A unspecified | → R7 |

## Rulings
- R1 Ruling: work on new branch hipodit-ld in the main tree with one foundation commit of the uncommitted root-project HiPoDiT files — a fresh worktree from HEAD would lose the untracked foundation the plan builds on — cost if wrong: one commit the user can `git reset --soft dd670c0` away.
- R2 Ruling: briefs hand-written in the workspace — the task-brief script only parses "Task N" headings — cost: none.
- R3 Ruling: test per-call NLL difference (B3−B0) is deterministic given the frozen decoder, so its 95% CI is a bootstrap over test individuals (2,000 resamples, fixed seed); LD r² MAE and other per-seed metric differences get a bootstrap CI over seeds — cost if wrong: CI semantics differ from the plan's wording; cheap to recompute.
- R4 Ruling: "real-vs-synthetic population-classifier" = multinomial logistic regression trained on real-train dosages; report accuracy and macro one-vs-rest AUC on real test and on synthetic samples with their conditioning labels — cost: if a discriminator was meant, one extra metric later.
- R5 Ruling: Phase 5 delivered as a results report docs/reports/hipodit_ld_20260915.md (formulas, tables, negative controls, strata, privacy, limits, allowed/forbidden sentences) rather than manuscript prose — cost: the user writes paper sections from it.
- R6 Ruling: study_manifest.json is written by the `oracle` CLI in scripts/hipodit_genotype_check.py — cost: none.
- R7 Ruling: λ_u, λ_A chosen on dev from the grid {1,10,100}² by B3 dev per-call NLL in Phase 1 (plan defers regularization choice to dev); B1/B2 reuse the chosen λ of their component — cost: 9 tiny convex fits.
- R8 Ruling: oracle metrics pool 20 sequential draws per dev individual to cut draw noise; end-to-end keeps the existing 1 draw per synthetic individual — cost: absolute values differ across protocols; gates compare arms within one protocol.
- R9 Ruling: LD "r²" = squared Pearson correlation of unphased dosages (composite LD); pairs = within-gene index offsets 1, 2, 4 (existing evaluator convention), stratified by the pair's physical distance using the train quartile edges (last bin open) — cost: none, documented.
- R10 Ruling: a missing (NaN) predecessor resets the chain like a gene boundary — dataset has no missing calls — cost: none.
- R11 Ruling: no Co-Authored-By trailers on commits (user memory rule overrides the harness reminder).

## Progress
- R12 Ruling: `/docs` is gitignored (plan file itself is untracked); T6 report goes to docs/reports/hipodit_ld_20260915.md like the existing hipodit_*.md reports, local-only, no force-add — cost: report not carried by the branch; user can `git add -f` if wanted.
- BASE for Task 1: c6b5f02. Task 1 dispatched (opus) at 2026-09-15T07:09:45Z.
- Task 1: implementer DONE, commit e1f624d, 25 new tests / 129 passed; review dispatched
- Task 1: minor (deferred): genotype_decoder.save appends .npz but load does not normalize suffix (callers must pass explicit .npz)
- Task 1: minor (deferred): `converged` = max|grad| <= 1e-6·n_observed_total is loose for minority cohorts (near-vacuous on the real panel, ~0.9 threshold)
- Task 1: minor (deferred): sample_calls gene-boundary reset not directly asserted (only nll/call_logits path is)
- Task 1: minor (deferred): no-leakage test omits train_nll_per_call; load() positional reconstruction; _checked_labels truncates float labels; nll_calls double-validates calls
- Task 1: complete (commits c6b5f02..e1f624d, review clean)
- BASE for Task 2: e1f624d. Task 2 dispatched (opus).
- R13 Ruling: Gate 2 AF/heterozygosity guardrail threshold = 105 % of B0 (mirrors Phase 3's AF criterion; plan leaves Phase 2 threshold unspecified) — cost if wrong: one number in evaluate_genotypes.
- Task 2: implementer DONE_WITH_CONCERNS, commit 0ceece5, 7 new tests / 136 passed; oracle run 1m02s CPU, 17 fits converged, λ=(1,1) grid corner, edges [35,104,210].
- Task 2 RESULT (dev, 20 draws): B0 nll .19706 af .00445 cohortAF .02085 tv .0752 het .0748 ldr2 .07676 | B1 .19404 .00511 .01926 .0756 .0748 .07620 | B2 .15086 .01329 .02604 .0360 .0329 .06270 | B3 .14561 .01370 .02413 .0354 .0317 .06039. Controls: label-shuffle B1 nll .19780 (≈B0, gain vanishes ✓), B3 .15181 (≈B2 ✓); order-shuffle B2 .15521 / B3 .15025 (real order better ✓ but ~90% of the A-gain is order-independent). GATE 1 FAILED: cohort AF ratio 1.157 > 1.05 → fixed_candidate B0.
- Task 2 concern (3) adjudicated: panel-wide validity assertions and the whole-panel label permutation read test rows but no test row influences any fitted parameter or reported metric — acceptable under Phase 0's "decoder input" wording; documented in report.
- Task 2 concern (2) adjudicated: λ at grid corner; plan forbids tuning after a gate failure; grid recorded as is.
- Task 2 review dispatched (opus), package review-e1f624d..0ceece5.diff
- R14 Ruling: Gate 1 FAILED (cohort AF MAE ratio 1.157 > 1.05) → follow plan header ("A gate that fails stops the downstream work; it is not a hyperparameter-tuning invitation"), Gate 1 text and §8 row 1: keep B0, do not modify diffusion, decoder novelty not adopted. Tasks 3/4/5 (Phases 2–4) are NOT executed; their briefs stay in the workspace unused. Task 6 report documents the negative result, the order-shuffle effect-size decomposition, the marginal-drift diagnosis (frozen α_j + per-SNP-centered u leave no unary freedom once A couples calls) and the B1 observation as OPTIONS for the user — no new arm is built without a plan revision. Cost if wrong: the user wanted iteration on the decoder; the module supports a per-SNP unary term with a small extension and Phases 2–4 briefs are ready.
- Task 2: review Approved (0 Critical). Important (plan-mandated): λ selected on dev by B3 NLL and the same dev decides Gate 1 → B3 dev NLL/LD optimistic; FAIL verdict conservative. Ruling: no code change (no third split except forbidden test); disclose in report and re-examine if a future arm passes narrowly — cost: none for this verdict.
- Task 2: minor (deferred): generated covariance uses all generated rows (diverges from legacy only with NaNs); no main()/subcommand dispatch; _gate1/order-control/un-permutation untested (test 6 vacuous); load_panel re-parses gene map; _evaluate_arm takes Namespace; ablation CSV header implicit; editing hipodit_genotype_check.py makes pilot manifest source hash STALE (pilot can't be re-summarised in place).
- Task 2: complete (commits e1f624d..0ceece5, review clean)
- CORRECTION to earlier diagnosis (controller check, dev): Binomial base UNDER-predicts heterozygotes (het pred−real −0.074; implied F ≈ −0.6 on train and dev); genotype class freq real [.692,.184,.124] vs B0 [.729,.111,.161]. Fitted A = mostly shared class term (+0.7..0.9 on g=1, −0.5..−0.8 on g=2) plus a diagonal (LD) part decaying with distance (diag−offdiag +0.66,+0.41,+0.33,+0.24 by bin). B3 matches pooled class freq exactly [.692,.183,.125] but per-SNP AF drifts toward 0.5 (79% of SNPs toward minor allele). Gene-start SNPs (no A) AF err .0051 vs chained .0139.
- User request (post-Gate-1): research how to remedy the cohort-AF failure. Literature agent dispatched → research-lit-af-drift.md; controller runs throwaway prototypes in scratchpad (no repo code change).

## Plan revision (user decision, 2026-09-15, after remedy research)
User accepts the remedy findings and re-defines the primary model as **HiPoDiT-T**: cohort-calibrated Binomial GLM-PCA latent diffusion + AF-preserving heterozygosity tilt, sampled INDEPENDENTLY per SNP (no A_b, no genomic-order chain). Novelty repositioned from "LD-aware diffusion" to "AF-preserving heterozygosity calibration in latent genotype synthesis". User's spec: q^T_ij(g) ∝ C(2,g) p^g (1-p)^(2-g) exp(τ_ij 1[g=1] + λ_ij g) with λ_ij solved so Σ_g g q^T = 2 p_ij; η_ij = α_j + u_{c_i,j} + v_j^T z_i; standard latent diffusion unchanged; decoder fitted on real train latents.
- R15 Ruling: implement the tilt in the existing src/models/genotype_decoder.py as a new arm (one module, one save format) rather than a new module — cost: module grows ~60 lines.
- R16 Ruling: τ granularity (per-SNP vs per-superpop×SNP vs per-cohort×SNP) decided by dev evidence from the af_tilt_variants prototype before the implementation brief is fixed — the user's τ_ij notation allows all three.
- Prototype evidence (dev, 5 seeds, 20 draws; B0 = 0.19706 / af .00445 / cohortAF .02104 / het .07490 / ldr2 .07681):
  B1 .194041/.00506/.01949/.07494/.07614 (gate-like 5/5) | B3 .145612/.01362/.02426/.03161/.06047 (0/5)
  E1 B3+delta_j .133698/.00474/.02002/.01913/.05428 (5/5) | B1+delta_jg .136266/.00460/.02020/.00952/.06123 (5/5)
  E2 B3+delta_jg .127974/.00473/.02010/.00975/.05215 (5/5) | E2_order_shuffle .133515/-/.02020/-/.05938
  T B1+tilt .107682/.00488/.01804/.01490/.03673 (5/5)  <- best, no dependence at all
  Order attribution with per-SNP class freedom present: NLL 66%, LD r2 80% order-dependent (vs ~10% for B3).
  KEY FINDING: a calibrated independent model (B1+delta_jg) already matches B3's LD r2 MAE → r2-MAE gains over B0 are mostly marginal calibration, not linkage.
- R16 resolved: tilt_scope default = "snp". dev NLL snp .107682 (361 params) < superpop .110666 (1805) < snp_noU .112317 < cohort .122852 (9386); cohort AF .01804/.01810/.01958/.01841; all gate-like 5/5. More granular τ is worse (49–91 train rows per cohort). U justified: NLL −.0046, cohortAF −.0015 vs T_snp_noU → "cohort-calibrated" = u_{c,j}, not τ.
- BASE for Task 7: 0ceece5. Task 7 (tilt decoder, arm "T") dispatched (opus).
- Plan file revised (rev.2): banner at top + §9 (Gate 1 record, HiPoDiT-T definition, new arm set, Gate 1′/2′/3′/4, allowed/forbidden sentences, file map). Original §1–§8 preserved as pre-registration history.
- Korean report written: docs/reports/hipodit_ld_tilt_20260915.md (diagnosis, literature, prototype tables, T definition, re-validation plan, limits). Prototypes + JSON copied to docs/reports/prototypes/.
- R17 Ruling: Gate 1′ drops LD r² MAE as a decision metric and adds heterozygosity MAE + overall AF MAE guardrail — independent calibrated models reduce r² MAE without modelling linkage, so r² MAE cannot adjudicate a linkage-free decoder — cost if wrong: one metric moves from "reported" back to "gating".
- R18 Ruling: new Task map for rev.2 — T7 tilt decoder (in flight), T8 oracle Gate 1′ with new arm set via repo CLI, T9 Phase 2 end-to-end GPU + Gate 2′ (task-3-brief adapted to T), T10 Phase 3 ≥10-seed paired B0/T + test once + bootstrap CI, T11 Phase 4 privacy/strata, T12 final Korean results report. Old T3–T6 briefs superseded.
- R19 Ruling: B1+δ_{j,g} and B3+δ_{j,g} stay prototype-only evidence (not implemented in repo code) — they are discussion ablations, not gate arms, and the prototype is anchored by exact reproduction of B0/B1/B3 dev NLL; plan §9.3 annotated — cost if wrong: the r²-confound counter-example is not regenerated by repo code (prototype scripts archived in docs/reports/prototypes/).
- R20 Ruling: Gate 1′ fixes λ_u = λ_τ = λ_A = 1.0 (CLI args, no dev grid search) — removes the dev-selection optimism the Task 2 review flagged as plan-mandated — cost: λ is not tuned; prototype grid showed the whole grid spans 0.005 NLL.
- R21 Ruling: oracle metrics now averaged over 5 sampling seeds (common random numbers across arms), gate judged on the mean with per-seed pass count recorded — the single-seed oracle could not separate draw noise from arm differences — cost: 5× sampling time (seconds).
- BASE for Task 8 will be Task 7's head. Task 8 brief written (Gate 1′ arm set, AF-preservation check, label-shuffle control, review-debt cleanup).
- Task 7: implementer DONE_WITH_CONCERNS, commit dbb1576, 19 new cases / 155 passed; dosage-preservation error <= 4.4e-16 (1.1e-15 at tau=+-30), tau recovery r=0.995, B0-B3 bitwise identical to pre-change snapshot, oracle decoder_B*.npz still load, 7 injected bugs all caught.
- R22 Ruling (Task 7 concern 1): my brief's test 6 was wrong — jointly fitted weight-centered U couples cohorts, so untouched tau rows cannot be bitwise equal (measured drift up to 0.064; ~150x smaller with U pinned). Accept the implementer's substitute: shape + routing equality + touched-row dominance (48-65x) — cost: the test asserts dominance rather than exact isolation.
- R23 Ruling (concern 2): raising ValueError when B0-B3 get a tilt_scope other than "none" is accepted as enforcing the brief's intent, not scope creep — cost: none.
- Task 7 review dispatched (opus), package review-0ceece5..dbb1576.diff
- Task 7: review Approved (0 Critical, 0 Important). Reviewer verified the closed-form root algebra by hand, the p<->1-p mirror, B0-B3 bitwise equivalence, backward-compatible load, and that no prototype numbers are hardcoded in tests.
- Task 7: minor (QUEUED, not deferred): no test pins the tau ridge — reviewer deleted `lambda_tilt*(tilt**2).sum()` and 43 tests still passed; U ridge under arm T also unpinned. Load-bearing because Gate 1' pre-registers lambda_tau=1. Fix = parametrize the existing stationarity test for T with lambda_tilt != lambda_u. Queued for the Task 7 implementer after Task 8 commits (different files, avoid a concurrent-edit race).
- Task 7: minor (deferred): spread**2 overflows for tau>~355 (torch.hypot would remove it); hand-built decoders can mismatch tilt shape vs tilt_scope without a check; sample_calls now materializes dense (N,J,3) logits for B0-B3 too (bitwise identical).
- Task 7: complete (commits 0ceece5..dbb1576, review clean)
- BASE for Task 8: dbb1576. Task 8 (Gate 1' oracle) dispatched (opus).
- Prototype complete. MP (tilt + Sinkhorn-projected A) dev NLL .101736 / cohortAF .01822 / het .01578 / ldr2 .02925, gate-like 5/5, Sinkhorn col err 8.4e-4; MP_order_shuffle .105135 / ldr2 .03599; MP0 (no tilt) .162501 / ldr2 .05691 / cohortAF .02015. Dependence on top of T: NLL +.0059 (57.2% order-dependent), LD r2 +.0075 (90.1% order-dependent), margins intact. Recorded as report §4.4 appendix, NOT in the pre-registered arm set.
- R24 Ruling: scripts/evaluate_synthetic_metrics.py carries a PRE-EXISTING uncommitted modification (mtime 2026-09-15 04:03, before this session; adds --stats-path/--real-space/--legacy-synthetic-space for the legacy eval scale correction described in docs/reports/hipodit_rebuild_execution_20260915.md). Left uncommitted deliberately — sweeping unrelated pre-existing work into this branch would misattribute it — cost if wrong: it stays dirty in the working tree; surface it to the user at handoff.
- Plan §9.4 Gate 3′ clarified: per-call NLL CI resamples test individuals; end-to-end metric CIs resample seeds (both 2,000 draws, fixed seed). Matches task-10-brief.
- Task 8: implementer DONE_WITH_CONCERNS, commit 92c3e0e, 174 passed; --arms legacy reproduces the frozen oracle byte-for-byte. GATE 1' FAILED on (d) overall AF MAE 0.00484566 > 1.05*0.00437701 (1.107x); (a) NLL .1076819 < .1970598, (b) het .0148225 < .0747411, (c) cohortAF .0179634 = 0.858x all pass.
- Controller diagnosis of (d): NOT the tilt. On train, B0 pooled AF MAE = 0.00000 (frozen alpha matches data by ML score equations) but adding U raises it to 0.00259; train-composition reweighting changes nothing => not composition. Cause = (1) Jensen: logit-space zero-mean U does not preserve mean sigma(.), (2) ridge blocks score-equation matching. Tilt alone (no U) has af_mae .00421 < B0 .00445. Uncentering U barely helps (1.090x vs 1.107x).
- User rejected all four options (amend gate / drop U / accept B0 / lower lambda) and asked for another method.
- SOLUTION FOUND (prototype, dev 5 seeds): constrain the cohort offset so the TRAIN pooled model AF equals the observed train AF per SNP, enforced during fitting by a per-SNP scalar shift solved with 25 Newton steps inside the objective (monotone; residual 2.2e-16). "T_pooled": NLL .107359, af_mae .004169 (0.938x B0), cohort_af .017577 (0.835x), het .014719, ld_r2 .03741. ALL FOUR Gate 1' conditions pass. Also beats centered T on NLL, af, cohort af, het.
- R25 Ruling: replace the logit-space weighted centering of u with this pooled-AF-preserving constraint. Justification is a priori, not result-driven: centering was adopted (plan §3.3) for identifiability against a FITTED intercept, but alpha is frozen here, and the gauge it picks does not preserve the probability-scale quantity the gate measures. Same design principle as the tilt (enforce the moment by construction instead of hoping a penalty respects it). Found after seeing Gate 1' fail, so dev numbers stay exploratory and confirmation must come from the untouched test split at Gate 3'. Cost if wrong: one gauge choice; "centered" stays available as an ablation.
- Research (research-cohort-calibration.md) validates R25: the pooled-AF constraint is constrained ML (Aitchison & Silvey 1958 eqs 2.1-2.2, one Lagrange multiplier per constraint); for an exponential family with canonical link the multiplier enters the canonical parameter additively (Berger et al. 1996 eqs 4/7/10), i.e. it IS a per-SNP logit offset delta_j — the same object the Newton solve finds. Equivalent construction: a free UNPENALIZED per-SNP intercept fitted jointly with the ridged u. Key derivation: pooled AF is the sum of the 26 cohort margins, so exact cohort matching implies exact pooled matching; the conflict exists only because the ridge blocks exact cohort matching. Survey-calibration analogue: Deming & Stephan 1940, Ireland & Kullback 1968, Sarndal 2007. Genetics analogue with E[p_cj]=p_j: Falush et al. 2003 eqs 4-5 (prior-expectation, not sample). Mean-pinning in ML: Saerens et al. 2002 eqs 4/9 (EM fixed point pins the sample mean). Not opened (low confidence): Deville & Sarndal 1992, Balding & Nichols 1995, ADMIXTURE 2009, Platt 1999.
- Task 12: implementer DONE, commit d847fe4, 183 passed; constraint residual 2.22e-16 (centered gauge drifts 2.25e-02 on the same panel); fit 6.1 s vs centered 1.1 s; all mutation checks bite (lambda_u, lambda_tilt, pooled-only ridge, post-hoc constraint, convergence guard).
- GATE 1' RE-RUN WITH REPO CODE: **PASSED 5/5 seeds**, outputs/diagnostics/hipodit_t_oracle_20260915_pooled/. (a) NLL T .107359 < B0 .197060; (b) het .0146226 < .0747411; (c) cohortAF .0175436 <= 1.05*.0209279 (0.838x); (d) overall AF .00413222 <= 1.05*.00437701 (0.944x). label_control_supports_cohort true. AF preservation: sampled vs expected max diff .00430 < draw noise .00669. Descriptive: LD r2 .0767 -> .0372 (all four bins improve), covariance .008446 -> .008129. Ablations: T_superpop .110341, T_cohort .122396 -> per-SNP tilt still best. All fits converged.
- LEDGER CORRECTION: the line claiming the Task 12 review was dispatched was written but no agent was launched; caught by a ListAgents check. Actually dispatched 2026-09-16 (opus), package review-92c3e0e..d847fe4.diff. Lesson: write the dispatch line only after the Agent call returns.
- FOLLOW-UP (minor): oracle_results.json does not record offset_gauge / pooled_af_residual per arm (CLI predates Task 12); the decoder .npz files do. Fold into the next task touching hipodit_genotype_check.py.
- Task 9: implementer DONE, commit 31d00c8, 196 passed (13 new); 12 mutation checks, 3 tests strengthened after surviving a broken implementation.
- GATE 2' PASSED (seed 20260327, dev, generated latents, 22.4 s GPU): het T .063237 < B0 .091457; cohortAF .069141 <= 1.05*.072920 (0.948x); af .025052 <= 1.05*.026459 (0.947x); nll_not_applicable true. scale_check clean (real_factor_max_abs_diff 0.0). Extremeness: generated |eta|>4 0.619 vs real 0.654 (ratio .947), mean_abs 5.47 vs 5.46 -> the tau/oracle-z over-confidence risk did NOT materialize. af_preservation gap .0312 < 3-sigma .0669 (loose at draws=1). B0 af_mae and covariance reproduce the same-seed pilot exactly.
- IMPORTANT for the write-up: decoder gains shrink end-to-end. het improvement 5.1x (oracle) -> 1.45x (end-to-end); cohort AF .0175 (oracle) -> .0691 (end-to-end). The dominant end-to-end error source is the latent generator, not the decoder.
- Task 9: minor (deferred): _load_gate1_decoder raises KeyError (not ValueError) when the oracle dir has no gate1_prime block, e.g. an --arms legacy directory.
- Task 9 review dispatched (opus), package review-d847fe4..31d00c8.diff
- SEQUENCING RULE: do not dispatch Task 10 (Phase 3, opens the test split once) until the Task 12 and Task 9 reviews are both clean.
- Task 9: review Approved (0 Critical, 0 Important). Independently confirmed: Gate 2' compares against the same run's B0 (checked in the artifact), and the B0 path is bit-unchanged vs the pre-change pilot at the same seed across EVERY shared key (only the four cohort_* keys added). Also confirmed generation labels == evaluation labels (same array, same slice, same order), scale check runs before any decoding (ordering proved by monkeypatching sample_calls to raise), and arm T carries no predecessor term.
- Task 9: minor (deferred): extremeness fractions asserted for presence only (threshold mutation survives); gate baseline wiring pinned via af_mae only; seed+1 and seed+3 reconstruction streams unpinned by tests; pooled_af_residual NaN from a pre-field decoder would crash _atomic_json (allow_nan=False) AFTER training+sampling — not reachable with the pinned decoder dir; label_composition uses list.count in a comprehension.
- Task 9: complete (commits d847fe4..31d00c8, review clean)
- Task 12: review Approved (0 Critical, 0 Important). Reviewer re-derived the reduced objective and the tangent projection independently and confirmed the item-9 stationarity test is the mathematically correct condition that a post-hoc mutant fails. Leak check clean: the solve closes over train rows only; scoring does no solve.
- Task 12: minor (deferred): converged fails open on a NaN residual (unreachable, torch.isfinite already catches); pooled_af_residual is not the model AF error for B3 (brief-specified; B1/T unaffected); NaN round-trip through JSON untested; all-train-rows LHS vs observed-rows RHS asymmetry (plan-mandated, 0 missing cells here); unrolled-graph memory not named in the ponytail comment (~145 MB/eval); naming nits.
- Task 12: complete (commits 92c3e0e..d847fe4, review clean)
- PRE-FLIGHT before opening test (reviewer recommendation): 0 monomorphic train SNPs, min train MAF 0.01024, 0 missing train cells, |cohort_offset| max 1.95 (p99 1.16), pooled residuals 3.3e-16 / 2.2e-16. The degenerate-column risk does not exist on this panel.
- BASE for Task 10: 31d00c8. Task 10 (Phase 3, opens the test split ONCE) dispatched (opus).
- Task 10: implementer DONE_WITH_CONCERNS, commit 33dc42d, 217 passed (21 new, 5 mutation checks). Run 236 s, 10 seeds, 0 failures, outputs/diagnostics/hipodit_t_multiseed_20260915.
- **GATE 3' PASSED on the TEST split (opened once)**: test NLL diff -0.091599 CI [-0.095635,-0.087636] (individual resample); cohort_af_mae -0.004570 CI [-0.004824,-0.004321] (seed resample); heterozygosity_mae -0.029448 CI [-0.030140,-0.028800] (seed resample); af_mae guardrail T 0.024214 <= 1.05*B0 0.024379. 10/10 seeds favour T on both gated end-to-end metrics.
- Task 10 concerns recorded: (1) two scripts total 489 lines vs the brief's 400 cap; (2) af_mae guardrail passes but its own CI straddles zero -> wording must be "does not degrade", never "improves"; (3) LD r2 improves but is descriptive only (independent sampling; §9.5 prohibitions); (4) population-classifier AUC T 0.855 vs B0 0.724 vs real 0.897 -> higher cohort attributability is fidelity here but is Gate 4's quantity; (5) the frozen schedule path's summarize() body was edited (shared per-run validation extracted, not copied) - review must treat it as a real edit.
- Task 10 review dispatched (opus), package review-31d00c8..33dc42d.diff
- Task 10: review Approved (0 Critical, 0 Important). Reviewer independently reproduced all 12 seed-resampled CIs to <1e-12 from paired_differences_decoder.csv, confirmed the NLL bootstrap resamples the 251 test individuals (ratio-of-sums statistic, n_calls 90611 = 251x361 exact), verified the extracted summarize() is behaviour-identical (ran the 10 frozen tests: 10 passed), confirmed 10/10 seeds ran once with no retry, the decoder was loaded not refitted (manifest digest matches), and refit the classifier to reproduce classifier_real_eval bit-for-bit.
- Task 10: minor (deferred): cohorts_excluded_from_auc discarded for synthetic arms (traceability only; all 26 cohorts present); local_pairs_evaluated/valid_genotype_fraction asserted for B0 only, not synthetic_T; --eval-split silently ignored in schedule mode; NLL denominator uses a second independently-defined missingness mask (agrees here, 0 missing); test-module fixture import coupling.
- Task 10: complete (commits 31d00c8..33dc42d, review clean)
- BASE for Task 11: 33dc42d. Task 11 (Phase 4 privacy + strata, Gate 4) dispatched (opus).
- Task 11: implementer DONE_WITH_CONCERNS, commit 25a4a3f, 224 passed (7 new, 6 mutation checks incl. an inverted-MIA-AUC check).
- **GATE 4 PASSED**: (a) MIA AUC T-B0 +0.0068 <= 0.02 (B0 .4927, T .4995 - both chance); (b) exact dup T 0.0 <= .003984; (c) near dup T 0.0 <= .015; (d) nn p1 T .0936 >= .0823 (0.8x B0 .1029). Zero exact/near/self duplicates in both arms across 10 seeds.
- Task 11 findings for the manuscript: T sits ~10% closer to the train panel than B0 at EVERY quantile, all 10 seeds, all 26 cohorts (median .1587 vs .1773). Passes (d) with 13.7% margin; closest synthetic (T min .0795) is still 3.6x farther from train than the closest real held-out person (.0222). This is the condition to watch if tilt strength is raised.
- Task 11 limitation recorded: the MIA setup is mildly favourable (synthetic conditioned on TEST cohort labels, so non-members match the synthetic composition while members are the whole train panel). A cleaner attack needs new samples, which Phase 4 must not generate.
- Task 11 strata: no cohort is `not estimable` or low precision (n_eval 6-11); T improves AF and het MAE in 26/26 cohorts; het gain concentrates in common SNPs (-37% in MAF [0.2,0.5]) with AF flat, as an AF-preserving tilt should. Phase 3 classifier AUCs carried as context_from_phase3 with used_in_gate4 false.
- Task 11 concern: scripts/hipodit_privacy.py 362 lines vs the brief's 300 cap (296 code).
- Task 11 review dispatched (opus), package review-33dc42d..25a4a3f.diff
- Task 11: review Approved with 1 IMPORTANT. Reviewer recomputed seed 20260327 from scratch (plain numpy + sklearn roc_auc_score on -distance) and reproduced B0 p1 .112188 / median .180055 / MIA .493215 and T .092798 / .160665 / .501477 exactly; reference p1 .03185595567867036 == near_duplicate_threshold. MIA orientation correct (small distance => member); member/nonmember sets not swapped; axis reductions correct; Gate 4 constants boundary-exact.
- Task 11 IMPORTANT (entering fix round 1): the member/non-member wiring at scripts/hipodit_privacy.py:114-115 has NO test — swapping train and test there passes the entire suite. The report's claim that the direction is "pinned from both sides" holds for the scoring function, not the wiring. Current wiring verified correct by the reviewer, so published numbers stand; the guard is what is missing.
- Task 11: minor (deferred): average() re-implements _across_seeds; mkdir runs before phase3_context's unguarded key access (malformed summary -> KeyError after dir exists, re-run then dies on FileExistsError); no cross-check Counter(eval_labels) == label_composition; threshold<-reference p1 wiring untested; ArgumentParser description=__doc__ is None; stale "6 passed" GREEN paste vs seven tests; docstring typo at :101.
- Task 11: fix round 1/5 (1 addressed, 0 open; commit amended 25a4a3f -> 6d8833c, +4 test lines). Re-reviewer hand-computed the fixtures: correct wiring gives AUC 1.0, swapped gives 0.0, so the assertion discriminates and is not a copied run value. Production script byte-unchanged (diff stat: tests only).
- Task 11: complete (commits 33dc42d..6d8833c, review clean after 1 fix round)
- ALL FOUR GATES PASSED: Gate 1' (oracle, dev, 5 seeds), Gate 2' (end-to-end, dev), Gate 3' (test split, 10 seeds, opened once), Gate 4 (privacy).
- BASE for Task 13: 6d8833c. Task 13 (Phase 5 final Korean results report, read-only, docs/ only, no commit) dispatched (opus).
- Task 13: DONE_WITH_CONCERNS. docs/reports/hipodit_t_results_20260916.md, 810 lines, all 13 sections. 400 value strings auto-matched against artifacts + a reverse pass over all 563 distinct decimal tokens: ZERO mismatches, zero unsourced figures. 17 SHA-256 computed fresh. Not committed (docs/ gitignored).
- BRIEF ERROR CORRECTED BY THE REPORT: my task-13 brief said "Phase 0 assertion 10개"; the artifact has 11 (the 11th is label_hierarchy_maps_every_cohort). Verified by the controller: 11 is right, the brief was stale. The report followed the artifact.
- Task 13 also corrected an attribution error I had propagated: "c = (1,-2,1) is the only AF-neutral direction" is a Derivation in the research note, NOT a Meisner & Albrechtsen statement. Fixed in the report's §2.3 and §9.1.
- Task 13 notes: Gate 1' final has no recorded wall clock (~82 s derived from created_utc gap, labelled as derived); order-shuffle LD r2 share recomputed from repo artifacts as 21.4%; 4 prototype-only figures labelled as such.
- FINAL whole-branch review dispatched (opus), package review-dd670c0..6d8833c.diff
- FINAL whole-branch review: "Ready to merge: With fixes". 0 Critical, 5 Important. Reviewer independently refit arm T (bit-identical to decoder_T.npz), re-ran the whole Gate 1' oracle at HEAD (byte-identical artifacts), measured fit gradients (1.18e-5 T / 9.8e-7 B1 vs a 0.72 threshold), and confirmed identical LD pair sets across arms (0 skipped).
- IMPORTANT 1: summarize_decoder raises ExperimentError when any seed's decoder_gate.passed is false, so a failing Gate 3' would crash instead of being recorded. Worst observed seed 20260333 af_mae ratio 1.0317 vs the 1.05 threshold - 1.7 points from converting a negative result into a crash + selective-stopping hazard.
- IMPORTANT 2: plan, reports, ledger and all artifacts are gitignored (.gitignore:35 outputs/, :58 /docs); merging carries code but not the pre-registration or verdicts. Also c6b5f02 DELETED two previously-tracked planning docs (2026-07-30 OC-FILM plan + spec) with no replacement - controller error I should have flagged.
- IMPORTANT 3: --max-variants recorded nowhere (not in prepare_report.json or study_manifest.json) and report §12.1 has no prepare command -> the frozen panel is unreproducible in principle.
- IMPORTANT 4: Gate 4's four constants live only in the untracked task brief; the code comment mis-attributes them to "Plan §5". Timestamps confirm they preceded measurement (brief 00:55, run 01:08) and concern quantities Gate 3' did not report, so no circularity - but provenance must be stated.
- IMPORTANT 5 (plan issue): all four gated metrics are quantities the decoder has a dedicated free parameter for; (b) het MAE is near-tautological for this model class. Report §8.2 reads the MAF-bin pattern as evidence against overfitting, but an overfit calibration would produce the same pattern.
- Reviewer triage: fix 5 deferred minors (stale pilot pin, loose converged threshold, B0-only degeneracy guards, near-duplicate threshold wiring untested, mkdir-before-unguarded-lookup); leave the rest with reasons. Confirmed the pooled_af_residual NaN crash is genuinely unreachable (only a centered-gauge decoder produces NaN, and _load_gate1_decoder refuses it since that run recorded passed:false).
- Fix wave dispatched (opus): Important 1,3,4,5 + the 5 triaged minors. Important 2 (version control policy) surfaced to the user.
- Fix wave: commit ffa47da, 229 passed (224 + 6 new - 1 retired case). Important 1,3,4,5 + all 5 triaged minors + the §6.3 deviation line fixed. Minor 2: convergence criterion is now relative-to-objective at 1e-7; all four fits still converge with 545x-14,282x margin (old bar 0.722722). Minor 4 mutation verified (p5 and the synthetic set's own p1 both fail the new test).
- Fix wave concern: ffa47da changed 3 files fingerprinted by the Gate 3' manifest, so verify_fingerprints now fails on hipodit_t_multiseed_20260915 as well as the pilot (verified clean at 6d8833c first). Artifacts and every prepared_sha256 unchanged; recorded in report §12.4. Nothing was regenerated.
- USER DECISIONS: (1) force-add the key documents, keep .gitignore rules as they are; (2) keep the OC-FILM plan/spec deletions from c6b5f02.
- Commit f0558c7 tracks: the plan, both reports, the two prototype scripts, and 22 gate record files (431 KB) including BOTH pre-registered failures (B3 Gate 1, centered-gauge Gate 1'). Bulk artifacts (checkpoints, .npz panels, sample tensors) stay ignored.
- Scoped re-review of the fix wave dispatched (sonnet), package review-6d8833c..ffa47da.diff

## Ponytail refactor (2026-09-16, post-final-review cleanup)

Scope: ~682 lines of abandoned scaffolding deleted, two .gitignore repository-safety defects
fixed, false claims corrected in CLAUDE.md and README.md. No Python logic, no number moved —
this repo holds frozen, published experimental results.

- R26 Ruling: the refactor SUPERSEDES half of the recorded USER DECISION (1) ("force-add the key
  documents, keep .gitignore rules as they are"). Commit f8b6364 dropped the `/docs` ignore rule,
  so the plan, the reports and this decision log are now tracked normally instead of by `git add -f`.
  Why safer: CLAUDE.md mandates that every judgement call be written into a decision log under
  `docs/reports/`, and an ignored `docs/` silently untracks exactly the artifacts the mandate
  requires — that is what produced final-review IMPORTANT 2 (the branch carried code but not the
  pre-registration or the verdicts). Force-add is a manual step and manual steps get forgotten;
  a fixed ignore rule cannot be. Cost if wrong: `docs/` now shows up in `git status`, so an
  incidental or bulky file dropped there can be committed by accident — mitigated by the standing
  rule that staging is always explicit and never `git add -A`.
- R26 SCOPE LIMIT (do not "fix" this later): the same USER DECISION also covers `outputs/`, and
  that half is DELIBERATELY UNTOUCHED. `.gitignore` still ignores `outputs/`; the 22 gate record
  files carried by f0558c7 are there by explicit force-add and stay that way. Bulk artifacts
  (checkpoints, .npz panels, sample tensors) must not become trackable by default. Anyone who
  later "completes" R26 by un-ignoring `outputs/` is reversing a decision, not finishing one.
- R27 Provenance (deleted PCA information-loss analyser): the refactor deleted
  `analyze_pca_information_loss` from `src/preprocessing/pca.py` (unreachable from the production
  pipeline, which fixes K = PCA_CANDIDATES[0] and never grid-searches). Its output still sits on
  disk at `data/processed/pca_information_loss_analysis.json` (131 KB, 2026-04-29), and `data/` is
  gitignored, so the artifact is local-only while the generating code now lives only in git
  history. Recovery command, recorded so the artifact never becomes orphaned:
  `git show 87f0c62:src/preprocessing/pca.py` (analyser at line 354, JSON path at line 396).
  Cost if wrong: none while the history survives; if history is ever rewritten the JSON loses its
  provenance and must be treated as an undated observation.
