# ───────────── Python ─────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
build/
dist/
.eggs/
.Python

# ───────────── Virtualenv ─────────────
.venv/
venv/
env/
ENV/

# ───────────── Tooling caches ─────────────
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pyre/
.pytype/
.ipynb_checkpoints/
.hypothesis/
.tox/
.coverage
.coverage.*
coverage.xml
htmlcov/

# ───────────── Editor / OS ─────────────
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# ───────────── Claude Code local settings ─────────────
.claude/

# ───────────── Training artifacts (huge) ─────────────
outputs/
models/
wandb/
logs/
/data/
_workspace/
checkpoints/
runs/

# ───────────── ver1 (legacy) data/experiment artifacts ─────────────
# Un-hidden by the /data/ anchor fix in 63abdc1; these are research
# data/checkpoint dumps, not source. ver1/data/ ~2.3GB, ver1/experiments/ ~105GB.
ver1/data/
ver1/experiments/

# ───────────── Experiment artifacts ─────────────
experiments/**/outputs/
experiments/**/outputs_*/
experiments/**/.cache/
experiments/**/wandb/
experiments/**/logs/
experiments/**/checkpoints/
experiments/**/runs/
experiments/**/cpt/
experiments/**/cpt_processed/

# ───────────── Logs & debug ─────────────
*.log
tea_debug.log

# ───────────── Large binary formats (model weights, datasets) ─────────────
*.pt
*.pth
*.bin
*.safetensors
*.ckpt
*.onnx
*.gguf
*.h5
*.hdf5
*.npz
*.npy
*.parquet
*.arrow
*.pkl
*.pickle

# ───────────── Secrets / local env ─────────────
.env
.env.*
!.env.example
*.pem
*.key

# ───────────── HuggingFace cache (if symlinked in) ─────────────
.cache/
huggingface/

# ───────────── 동의보감 음성 자비스 작업 문서 (로컬 전용) ─────────────
experiments/dongui_bogam/docs/
