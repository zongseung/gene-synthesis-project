"""이미지 part(영문) ↔ 약용부위(한글) 어휘 — 단일 진실.

전에는 이 사전들이 `stage2_vlm/build_herb.py` 와 `scripts/validate_sft_mm.py` 에
각각 복사돼 있었고("빌더와 동일해야 한다"는 주석만으로 손으로 맞춰야 했다),
`scripts/build_part_audit.py` 에도 세 번째 사본(`PART_KO`)이 있었다.

이미 한 번 어긋났다 — 검증기 사본에 `whole` 이 빠져 있어 전초 사진의 부위
캐비엇 검사가 통째로 건너뛰어졌다. 세 곳 모두 여기서 임포트한다.
"""
from __future__ import annotations

# 이미지 part(영문) → 한글 표기.
# group 은 부위가 아니라 군락(여러 개체) 사진이다 — 전초(한 개체 전체)와 다르다.
# 실물 확인: 612/부들 group 은 연못의 부들 군락 사진이었다.
PART_KO = {"root": "뿌리", "stem": "줄기", "leaf": "잎",
           "flower": "꽃", "fruit": "열매",
           "whole": "전초", "group": "군락"}

# 부위가 아니라 사진 유형인 값. 답변에서 부위로 단정하지 않는다.
NON_PART = ("group", "whole")

# 이미지 part(영문)와 호환으로 간주할 약용부위(한글) 집합 — 부위 불일치 캐비엇 판단용.
# 부분문자열보다 안전한 명시 매핑(예: root는 뿌리/뿌리줄기/줄기껍질/수피 와 관련).
# group(전초/군락 사진)은 식물 전체가 찍히므로 모든 부위와 호환(캐비엇 생략).
ALL_PARTS_KO = {"뿌리", "뿌리줄기", "줄기껍질", "수피", "줄기",
                "잎", "꽃", "열매", "씨", "껍질", "전초"}

PART_COMPAT = {
    "root": {"뿌리", "뿌리줄기", "줄기껍질", "수피"},
    "stem": {"줄기", "뿌리줄기", "줄기껍질", "수피", "껍질"},
    "leaf": {"잎", "전초"},
    "flower": {"꽃", "전초"},
    "fruit": {"열매", "씨", "껍질"},
    # 군락·전초 사진은 식물 전체가 찍혀 어떤 약용부위와도 어긋나지 않는다 → 캐비엇 생략.
    # whole 이 빠지면 전초 사진마다 「사진은 whole으로 보입니다」가 나온다 (실제 발생 이력).
    "group": ALL_PARTS_KO,
    "whole": ALL_PARTS_KO,
}
